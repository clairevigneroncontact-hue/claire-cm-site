---
interface Props {
  text?: string;
}
const { text = 'Places limitées — Appel découverte gratuit 30 min · Réserver maintenant' } = Astro.props;
---

<div class="bg-[#2C2416] text-[#F5EDE4] text-xs font-medium py-3 text-center tracking-wide uppercase px-4">
  <span>{text}</span>
</div>
